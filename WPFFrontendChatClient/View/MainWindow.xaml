<Window x:Class="WPFFrontendChatClient.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFFrontendChatClient.View"
        mc:Ignorable="d"
        DataContext="{Binding Main, Source={StaticResource Locator}}"
        Title="Chat Window" Width="1280" Height="720" MinWidth="1280" MinHeight="720" MaxWidth="1280" MaxHeight="720"
        WindowStartupLocation="CenterScreen" ResizeMode="CanMinimize">
    <Grid>
        <DockPanel x:Name="EntrarPanel">
            <Button Click="ButtonEntrar_Click" Content="Entrar" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </DockPanel>
        <DockPanel x:Name="ChatPanel" Visibility="Collapsed">
            <TabControl TabStripPlacement="Top" DockPanel.Dock="Left" VerticalAlignment="Top" Width="150"
                        Margin="10,10,10,10">
                <TabItem>
                    <TabItem.Header>
                        <TextBlock Text="Aulas" FontWeight="Bold" />
                    </TabItem.Header>
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                        <Grid>
                            <ItemsControl x:Name="AulasItemsControl">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="20" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding UnidadeCurricular.Nome}" Grid.Column="0"
                                                       Margin="5,2,2,2" />
                                            <Button Content="➡" Grid.Column="1" HorizontalAlignment="Right"
                                                    Background="Transparent" BorderThickness="0" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </ScrollViewer>
                </TabItem>
                <TabItem>
                    <TabItem.Header>
                        <TextBlock Text="Utilizadores" FontWeight="Bold" />
                    </TabItem.Header>
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                        <Grid>
                            <ItemsControl x:Name="UsersItemsControl">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="20" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Nome}" Grid.Column="0" Margin="5,2,2,2" />
                                            <Button Content="✉" Grid.Column="1" HorizontalAlignment="Right"
                                                    Background="Transparent" BorderThickness="0" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </ScrollViewer>
                </TabItem>
            </TabControl>
            <Grid DockPanel.Dock="Top" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10,10,10,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="TextBlockUtilizadorLogado" Grid.Column="0" VerticalAlignment="Center"
                           FontWeight="Bold" />
                <Button Click="ButtonSair_Click" Content="🚪" Grid.Column="1" FontSize="15" Background="Transparent"
                        BorderThickness="0" Margin="10,0,0,0" />
            </Grid>
            <Grid DockPanel.Dock="Bottom" VerticalAlignment="Center" Margin="10,10,10,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Name="TextBoxMensagem" Grid.Column="0" Height="Auto" MaxHeight="50" AcceptsReturn="True"
                         SpellCheck.IsEnabled="True" Language="pt-PT" TextWrapping="Wrap" HorizontalAlignment="Stretch"
                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" />
                <Button Name="ButtonEnviar" Content="Enviar" Grid.Column="1" ClickMode="Press" Margin="10,0,0,0"
                        Width="50" HorizontalAlignment="Left" />
            </Grid>
            <StackPanel Margin="10,0,10,0" VerticalAlignment="Bottom">


                <Label Content="ZONA DE TESTES" Margin="0,0,0,-5" FontFamily="Segoe Ui" FontWeight="Bold"
                       VerticalAlignment="Center" />
                <Button Click="AdicionarUtilizadorTeste_OnClick" Content="Adicionar Utilizador (Teste)" />


                <TextBlock Foreground="Black" FontSize="15" Text="MSG 1" />
                <TextBlock HorizontalAlignment="Right" Foreground="Black" FontSize="15" Text="MSG 2" />
                <TextBlock Foreground="Black" FontSize="15" Text="MSG 3" />
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>